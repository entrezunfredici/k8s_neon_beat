# =============================================================================
# Secret Cloudflare API Token - cert-manager DNS01
#
# Do not commit a real token here. Replace the placeholder locally, create the
# secret with kubectl, or use Sealed Secrets / External Secrets in production.
# =============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-api-token
  namespace: cert-manager
type: Opaque
data:
  # base64("change-me") - replace before real use.
  api-token: Y2hhbmdlLW1l
